<div class="body">
      <div class="container">
        <p class="gr-index"><%=  notice %></p>
        <div class="row">
          <div class="top-message col-md-offset-4">
            <div class="btn-wrapper">
              会員の方はこちらからログイン
                <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

                    <div class="field">
                      メールアドレス
                      <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
                    </div>
                    <br>

                  <div class="field">
                    パスワード　　<!---->
                    <%= f.password_field :password, autocomplete: "current-password" %>
                  </div>

                    <%= link_to "＝＞パスワードをお忘れの方はこちら", new_password_path(resource_name) %><br />


                  <% if devise_mapping.rememberable? %>
                    <div class="actions">
                    <input type="submit" name="commit" value="ログイン" class="btn btn-primary" data-disable-with="Log in">
                    </div>
                  <br>
                  <% end %>
                  登録がお済みでない方
                  <br>
                    <%= link_to "こちら", new_registration_path(resource_name) %>から新規登録を行ってください。
            </div>
            <% end %>
      </div>
    </div>
  </div>
</div>