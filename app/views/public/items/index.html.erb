<div class="container">
<h2>商品一覧</h2>
<% @items.each do |item| %>
  <%= attachment_image_tag item, :image, fallback:"no_image.jpg", size: '200x100' %>
  <div><%= item.name %></div>
  <div><%= item.price %></div>
<% end %>
</div>
